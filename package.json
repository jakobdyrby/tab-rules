{
  "name": "tab-rules",
  "version": "0.1.0",
  "private": true,
  "description": "URL-based automatic tab groups for Chrome",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "check": "node scripts/check.mjs",
    "package": "node scripts/package.mjs"
  }
}
